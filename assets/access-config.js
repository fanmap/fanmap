/* Public project settings only. Never put service-role keys or passwords here.
   Keep disabled until the database policies, Edge Function and verified-email
   signup are deployed to the owner's connected Supabase project. */
window.FANMAP_ACCESS_CONFIG = Object.freeze({
  enabled: false,
  url: '',
  publishableKey: ''
});
